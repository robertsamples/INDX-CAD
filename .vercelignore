# Vercel serves the "site" folder as the web root (Project Settings -> Root Directory).
# These CAD sources live on GitHub only; keeping them out saves ~185 MB per deploy.
*.f3z
*.step
*.stp
*.3mf
*.zip

# OG card source (regeneration only, not served)
tools/
